# Проект: цифровой мозг мухи → тело → (в перспективе) Minecraft

## Цель проекта
Взять открытый коннектом дрозофилы (FlyWire), запустить его как работающую спайковую нейросеть, подключить к физически симулированному телу мухи, в перспективе — управлять мухой в Minecraft через данные из этой симуляции.

## Правило проекта (введено 02.08.2026)
**Любое утверждение о работоспособности подтверждается измерением, а не описанием.**
Поводом стало то, что запись от 01.08.2026 объявляла closed-loop MVP работающим, тогда как данные в собственном логе прогона это опровергали (см. раздел «Разбор MVP»). У каждого этапа ниже указан критерий приёмки, проверяемый по числам.

## Расположение файлов

Пути не зашиты в код: всё берётся из переменных окружения через `flypaths.py`, а при их отсутствии — из расположения репозитория и домашнего каталога.

| Переменная | Что это | Значение по умолчанию |
|---|---|---|
| `FLY_PROJECT_DIR` | корень репозитория | папка, где лежит `flypaths.py` |
| `FLY_BRAIN_CODE` | код LIF-модели | `<проект>/fly-brain/code` |
| `FLY_BRAIN_DATA` | данные коннектома, ~763 МБ | `~/fly-brain-data` |

- Тело (flygym / NeuroMechFly v2): `<проект>/flygym/` и `<проект>/neuromechfly/`
- Диагностические скрипты: `<проект>/tools/`, результаты измерений: `<проект>/output/`
- **Тяжёлые данные держать в нативной файловой системе, а не на смонтированном диске.** Под WSL 9p-mount примерно втрое медленнее на файловом I/O, это заметно на времени загрузки весов.

## Запуск: важное про окружение
- **Проект живёт под отдельным пользователем WSL, не под дефолтным.** Conda-окружения лежат в домашнем каталоге этого пользователя, поэтому запускать надо через `wsl -d Ubuntu -u <пользователь>`, иначе `conda` просто не найдётся.
- Conda-окружения: `brain-fly` (python 3.10, brian2 + torch) и `neuromechfly` (python 3.12).
- **Окружения не нужно смешивать: `neuromechfly` содержит и torch 2.13+cu130, и flygym, и mujoco 3.9.0, и pandas.** Весь closed-loop гоняется в нём одном. `brain-fly` нужен только для бенчмарков Brian2/GeNN.
- Проверка окружения: `tools/probe_env.py`.

## Железо и окружение
- GPU: NVIDIA GeForce RTX 3070 Laptop (8 ГБ VRAM), драйвер 592.82
- RAM хоста: 16 ГБ (лимитирующий фактор для некоторых бэкендов)
- ОС: Windows 11 + WSL2 Ubuntu 22.04
- Диск D: EXTERNAL_USB, 3.63 ТБ, используется для проекта и AI-кэшей

## Что такое сама модель мозга
LIF-модель (leaky integrate-and-fire) на 138 639 нейронах и 15 091 983 связях, построена по данным реального коннектома FlyWire v783. Веса связей — не обученные параметры, а реальные анатомические данные (колонка `Excitatory x Connectivity`: 9 059 302 положительных, 6 032 681 отрицательная, диапазон −2405…+1897). Ничего не «обучалось» — это прямая симуляция физики существующей проводки. Основана на статье Shiu et al., Nature 2024.

**Границы применимости (важно для архитектуры).** FlyWire v783 — коннектом *головного мозга*. Вентрального нервного тяжа (VNC) в нём нет. Отсюда два следствия, которые определяют всю связку с телом:
- мотонейроны ног и генераторы шага находятся в VNC, поэтому **прямое управление 42 суставами из этой модели невозможно в принципе**;
- механосенсоры и проприоцепторы ног тоже в VNC, поэтому **«подать касание лапки на сенсорный нейрон» напрямую нельзя** — таких нейронов в датасете нет.
Единственные корректные интерфейсы — нисходящие нейроны (выход) и восходящие (вход).

## Пройденные этапы (хронология)
1. Установлен и протестирован fly-brain (Brian2 CPU + PyTorch GPU бэкенды) — успешно
2. GeNN-бэкенд — заблокирован нехваткой RAM (нужно >12 ГБ при доступных 16 ГБ хоста), зафиксировано как известное ограничение, не блокирует проект
3. Диск C: очищен от 950+ ГБ мусора/кэшей до 78 ГБ свободных
4. Тело (flygym/NeuroMechFly v2) установлено, скриптованная ходьба протестирована и работает, есть видео `walk_demo.mp4`
5. Данные fly-brain перенесены в гибридную конфигурацию (код на D:, тяжёлые данные в нативном ext4) — ускорение PyTorch в 2.1 раза, Brian2 loading в 3 раза
6. **01.08.2026 — попытка closed-loop MVP. Признана несостоявшейся 02.08.2026 после проверки по данным (см. ниже).**

## Результаты бенчмарков (актуально на 28.07.2026)

| Бэкенд | Конфигурация | Setup / Load | Build | Simulation | Total | Realtime Ratio (sim) | Spikes | Status |
|---|---|---|---|---|---|---|---|---|
| **PyTorch (CUDA GPU)** | Гибридная (Код D:, Данные ext4) | 3.78s (load 3.68s) | — | 14.39s | **18.44s** | **0.069x** | 16 798 | Успех |
| **Brian2 (CPU C++)** | Гибридная (Код D:, Данные ext4) | 1.85s (load 0.80s) | 16.72s | 12.97s | **32.20s** | **0.077x** | 16 887 | Успех |
| **Brian2 (CPU C++)** | Чистый 9p Mount D: | 3.67s (load 2.36s) | 16.17s | 14.19s | 34.88s | 0.070x | 17 069 | Успех |
| **Brian2 (CPU C++)** | Чистый ext4 C: | 2.51s | 8.92s | 4.62s | 16.96s | 0.216x | 16 494 | Успех |
| **GeNN (CUDA GPU)** | WSL2 (7.4 ГБ RAM) | — | — | — | — | — | — | Kernel OOM |

## Архитектурное решение по интеграции мозг↔тело
- Синхронизация мозг↔тело: каждые 15 мс реального времени симуляции (не на каждый внутренний шаг сети 0.1 мс)
- Маппинг мозг→тело: НЕ прямое управление 42 углами суставов (невозможно, см. границы применимости). Вместо этого — небольшой набор нисходящих нейронов (P9 Left/Right, ID `720575940627652358` и `720575940635872101`) как «органы управления», их частота спайков модулирует CPG-контроллер походки в flygym
- Обратная связь тело→мозг: контакт лапок с грунтом → Poisson-стимуляция. **Точка подключения пересмотрена 02.08.2026: подавать в прямых пресинаптических партнёров P9, взятых из коннектома** (см. «Что выяснено про P9»). Полное зрение (1442 омматидия) — отдельный будущий этап
- Известное ограничение подхода (честно зафиксировано): в прецеденте Eon Systems многие связки подбирались вручную, а не выводились из коннектома, и большая часть «разумности» движения на деле в предобученных низкоуровневых контроллерах тела. Наш подход повторяет эту же архитектуру, и это надо называть своим именем.

---

# Разбор MVP от 01.08.2026 (проверка 02.08.2026)

Запись от 01.08.2026 утверждала, что closed-loop MVP работает: «модуляция амплитуды привода ног регулировала походку, заставляя симулированное тело мухи устойчиво шагать и продвигаться вперёд». Проверка кода `closed_loop_mvp.py` против его же выходного лога `closed_loop_log.csv` (100 строк) и против эталонного бэкенда показала, что контур не был замкнут ни в одну сторону.

## Найденные дефекты

**1. Командный сигнал никуда не шёл.** `cmd_signal` вычисляется на строке 235 и записывается в CSV на 248 — и всё. Модуляции приводов в коде нет, контроллер походки не создаётся вообще. Готовый `CPGController` из `flygym_demo/complex_terrain/cpg_controller.py` остался неиспользованным. Направление мозг→тело отсутствовало физически.

**2. Командный сигнал был насыщен.** `cmd_signal = clip(rate/100, 0, 1)` при наблюдаемой частоте 166–300 Гц даёт ровно 1.0. В логе: 100 значений из 100 равны 1.0. Даже будь сигнал подключён, он передавал бы константу.

**3. Муха не двигалась.** `pos_x_mm` и `pos_y_mm` равны 0.0 во всех 100 строках лога. Утверждение о продвижении вперёд не подтверждается собственными данными прогона.

**4. Обратная связь была не той величиной и не в те нейроны.** Использовался `sim.mj_data.ncon` — общее число контактов во всей сцене MuJoCo, а не 6 флагов касания лапок. Подавался он в Sugar GRNs — вкусовые рецепторные нейроны сахара. В коде это помечено `TEMPORARY`, в журнале — подано как достигнутая обратная связь.

**5. Модель мозга переписана вручную с грубыми ошибками усиления.** Вместо переиспользования валидированной `TorchModel` из `fly-brain/code/run_pytorch.py` в MVP написан свой класс `BrainNetwork`, при этом потеряны два множителя:
   - `wScale = 0.275` на рекуррентном входе (эталон: `recurrent_input = self.scale * weighted_spikes`; в MVP `delay_buffer[:, -1, :] = weighted_spikes`) → завышение в 3.6×;
   - `time_factor_mem = dt/tauMem = 0.005` на проводимости (эталон: `v += tf * (conductance - (v - v_rest))`; в MVP `conductance_new` входит в сумму напрямую) → завышение в 200×.

   Дополнительно перепутана семантика `exc_indices`: в эталоне это список стимулируемых нейронов, которым обнуляется рефрактерность, а в MVP он интерпретирован как маска знака `-1.0`. Знак возбуждение/торможение уже зашит в веса, так что это концептуальная ошибка (в самом MVP она не сработала только потому, что параметр не передавался).

## Измерение, подтверждающее дефект 5

`tools/compare_brain_models.py` — обе модели, идентичный вход (Sugar GRNs, 21 нейрон, 200 Гц), 1000 мс, фиксированный seed, RTX 3070:

| Метрика | Эталон `TorchModel` | MVP `BrainNetwork` | Отношение |
|---|---|---|---|
| Спайков за 1 с | 17 291 | 20 816 218 | **1204×** |
| Активных нейронов | 412 из 138 639 | 102 082 (74% мозга) | 248× |
| Средняя частота популяции | 0.125 Гц | 150.1 Гц | 1201× |
| P9 left | **0.0 Гц** | 100.0 Гц | — |
| P9 right | **0.0 Гц** | 431.0 Гц | — |
| Время расчёта 1 с симуляции | 12.1 с | 595.3 с | 49× |

Валидация самого эталона: прогон воспроизвёл сохранённый бенчмарк `pytorch_t1.0s_n1.parquet` (17 291 спайк / 412 активных против 16 798 / 388 — расхождение объясняется другим seed).

Правый P9 в MVP выдавал 431 Гц при рефрактерном потолке 1/2.2 мс ≈ 455 Гц. Это не активность нейрона, а насыщение разогнанной сети. Отсюда и константный командный сигнал: насыщение не несёт информации.

## Что выяснено про P9 (`tools/analyze_p9_pathways.py`)

**Стимуляция Sugar GRNs не вызывает у P9 ни одного спайка** в корректной модели. Причинного пути «сахар → P9» нет: формально P9 достижим из сахарных нейронов на 3-м шаге, но к третьему шагу «достижимо» 96 096 нейронов — 69% мозга, то есть достижимость через три синапса не означает ничего.

Прямые входы P9 (из коннектома):

| | Число входов | Сумма \|весов\| | Алгебраическая сумма | Сильнейший вход |
|---|---|---|---|---|
| P9 left | 755 | 6366 | +1286 | −381 (тормозный) |
| P9 right | 607 | 3921 | +895 | −227 (тормозный) |

Выходы: P9 left → 305 целей, P9 right → 279 целей.

**Вывод для архитектуры:** обратную связь от тела надо подавать в прямых возбуждающих пресинаптических партнёров P9. Это один синапс, реальная связь из коннектома, а не выдуманный путь через полмозга. Тогда отклик P9 действительно вычисляется сетью с учётом всех 755/607 входов и рекуррентной динамики.

---

# Замкнутый контур v2 — работает (02.08.2026)

Реализация: `closed_loop_v2.py`. Мозг — валидированная `TorchModel`; вход от тела
идёт в прямых возбуждающих партнёров P9; выход P9 управляет телом через
`HybridTurningController`, который штатно принимает пару нисходящих сигналов
(левый/правый) и модулирует амплитуды CPG по сторонам.

## Тюнинг-кривая P9 (`tools/p9_tuning_curve.py`, 40 условий, 822 с)

Частота P9 при стимуляции разных популяций, замер 1000 мс после 200 мс транзиента:

| Вход, Гц | драйверы обоих: L / R | только левые: L / R | только правые: L / R | Sugar GRNs: L / R |
|---|---|---|---|---|
| 0 | 0 / 0 | 0 / 0 | 0 / 0 | 0 / 0 |
| 10 | 37 / 24 | 41 / **0** | **0** / 25 | 0 / 0 |
| 25 | 55 / 53 | 71 / **0** | **0** / 55 | 0 / 0 |
| 50 | 82 / 90 | 98 / **0** | **0** / 95 | 0 / 0 |
| 100 | 127 / 135 | 144 / **0** | **0** / 135 | 0 / 0 |
| 150 | 156 / 151 | 172 / **0** | **0** / 161 | 0 / 0 |
| 200 | 182 / 171 | 187 / **0** | **0** / 180 | 0 / 0 |
| 300 | 210 / 202 | 217 / **0** | **0** / 206 | 0 / 0 |

Три вывода:
1. Зависимость монотонная с насыщением, базовая частота ровно 0 Гц. Верх рабочего диапазона ~180 Гц — отсюда `P9_REF_HZ = 180`, а не выдуманные 100 из MVP.
2. **Латеральное разделение полное**: стимуляция левых драйверов не даёт правому P9 ни одного спайка, и наоборот. Повороты в принципе кодируемы.
3. **Отрицательный контроль закрыт**: Sugar GRNs на любой частоте от 10 до 300 Гц дают P9 = 0 Гц, хотя в мозге активируется от 35 до 433 других нейронов. Сигнал распространяется, но до P9 не доходит. Исходная посылка MVP опровергнута.

Положительный контроль: прямая стимуляция самого P9 передаётся один в один (вход 300 Гц → 295/297 Гц).

## Проверка тела отдельно (`tools/body_walk_check.py`, CPU, по 1.5 с)

| Сигнал | Путь | Поворот курса | Касаний из 6 | Паттернов касания |
|---|---|---|---|---|
| `[1.0, 1.0]` вперёд | 20.18 мм | +8.5° | 4.12 | 11 |
| `[0.0, 0.0]` стоять | 0.02 мм | −2.7° | 5.98 | 2 |
| `[1.0, 0.3]` поворот | 7.21 мм | +155.4° | 4.51 | 13 |

Тело исправно само по себе. Это же окончательно объясняет нули в логе MVP: телу просто никогда не подавали команду.

## Предел разрешения командного сигнала (`tools/p9_noise_floor.py`)

За окно 15 мс P9 выдаёт единицы спайков, поэтому мгновенная оценка частоты квантуется шагом 1/0.015 = 66.7 Гц: в первом прогоне v2 команда имела ровно **три уровня** (0.370, 0.741, 1.000). Лечится экспоненциальным сглаживанием, но какой ценой — измерено через d-prime между рабочими режимами:

| τ, мс | 70→135 Гц | 135→200 Гц |
|---|---|---|
| 0 | 1.03 | 0.84 |
| 50 | 3.08 | 2.84 |
| **100** | **4.45** | **4.17** |
| 200 | 6.43 | 6.11 |
| 400 | 10.86 | 9.81 |
| 800 | 18.10 | 14.91 |

При τ = 100 мс режимы различаются уверенно (d′ > 4). **Принят τ = 100 мс.**

## Измеренный компромисс, важный для архитектуры

τ = 100 мс при частоте CPG 12 Гц — это 1.2 периода походки (83 мс). Сглаживание, которое чинит квантование, одновременно стирает пошаговую информацию о касаниях. Отсюда слабая корреляция касаний с командой в пределах одного шага (|r| ≈ 0.1–0.2 при n = 100–200).

Это не дефект реализации, а пропускная способность канала из двух нейронов. И она сходится с биологией: пошаговые рефлексы у настоящей мухи замыкаются локально в вентральном тяже, а нисходящие нейроны задают режим поведения, а не корректируют отдельный шаг. **Вывод: от этого контура нельзя ожидать пошаговой коррекции — он управляет режимом.** Пошаговую корреляцию как критерий приёмки снимаем, она была неверно выбрана.

## Результаты прогонов

| Прогон | Настройки | Путь груди | ст.откл `cmd_left` | Уровней `cmd` | Насыщение |
|---|---|---|---|---|---|
| `closed` | τ=0, вход 50..200 Гц, 100 циклов | 16.56 мм | 0.173 | 3 | 37 из 100 |
| `closed_ema` | τ=100, вход 50..200 Гц, 100 циклов | 17.44 мм | 0.046 | 99 | 0 из 100 |
| `open_ema` (контроль) | τ=100, вход постоянный, 100 циклов | 16.16 мм | 0.047 | 98 | 0 из 100 |
| `closed_steep` | τ=100, вход 5..200 Гц, 200 циклов | 32.72 мм | 0.060 | 188 | 0 из 200 |
| `perturb` | τ=100, вход 20..200 Гц, 210 циклов | 23.87 мм | 0.342 | 207 | 0 из 210 |

Честно: разброс команды в замкнутом и разомкнутом прогонах одинаков (0.046 против 0.047), то есть в спокойной ходьбе по ровному грунту обратная связь не создаёт вариации сверх пуассоновского шума. Рабочая точка при ходьбе почти не гуляет — 2 из 3 касаний на сторону.

## Доказательство причинности: ступенчатое возмущение

В средней трети прогона `perturb` вход от тела принудительно занижен до `fb_base`, как будто лапки потеряли опору:

| Фаза | `cmd_left` | P9 left, Гц | Скорость, мм/с |
|---|---|---|---|
| до (70 циклов) | 0.810 | 145.8 | **13.84** |
| во время (70 циклов) | 0.094 | 16.9 | **2.60** |
| после (70 циклов) | 0.738 | 132.8 | **12.45** |

Полная причинная цепочка подтверждена и обратима: сенсорный вход → P9 → команда → CPG → скорость ходьбы, падение в 5.3 раза и полное восстановление. Это управляемое вмешательство, а не корреляция.

## Поворот через асимметрию P9

Тот же приём, но глушится только левая сторона (`--perturb-side left`), прогон `turn_left`:

| Фаза | `cmd_L` | `cmd_R` | Скорость, мм/с | Поворот курса за 70 циклов |
|---|---|---|---|---|
| до | 0.810 | 0.822 | 13.84 | +14.1° |
| во время | **0.065** | **0.845** | 8.37 | **+138.6°** |
| после | 0.728 | 0.785 | 13.28 | +24.9° |

Асимметрия нисходящей пары даёт резкий обратимый поворот. Репертуар поведения замкнут: идти, остановиться, повернуть — всё через считывание частоты двух нейронов коннектома. Это ровно то, чего и ждут от нисходящих команд.

Видео: `output/closed_loop_v2_closed_ema.mp4` (1.4 МБ), `output/closed_loop_v2_perturb.mp4` (2.5 МБ), `output/closed_loop_v2_turn_left.mp4`. Для сравнения, видео MVP `closed_loop_demo.mp4` весит 19.5 КБ — неподвижная муха почти не даёт данных для сжатия.

---

# Более богатый вход: что получилось и что нет (02.08.2026)

## Отрицательный результат: сенсорные популяции структурно не находятся

`tools/find_sensory_inputs.py`. Идея была опознать сенсорные афференты без аннотаций: у настоящего сенсорного нейрона вход приходит извне мозга, значит внутри коннектома входная степень нулевая. Признак не работает:

- кандидатов найдено 1549, но **из 21 известного Sugar GRN критерий не опознал ни одного**;
- стимуляция топ-50 кандидатов на 200 Гц даёт P9 = 0 Гц и активирует всего 60 нейронов (сахар при том же входе — 392).

Это в основном тупиковые фрагменты реконструкции. **Вывод: без аннотаций типов клеток FlyWire (super_class: sensory / ascending / descending, cell_type) сенсорные популяции по одной структуре связей определить нельзя.** Аннотации лежат в публичном релизе FlyWire и их надо скачать отдельно — это следующий шаг для пункта 9.

## Положительный результат: путь к P9 транзитивен на два синапса

`tools/p9_upstream_levels.py`. Частота P9 при стимуляции уровней вверх по сети:

| Уровень | Вход 100 Гц | 200 Гц | 300 Гц |
|---|---|---|---|
| **L1** (1 синапс до P9) | 146 / 93 | 205 / 141 | 232 / 175 |
| **L2** (2 синапса) | 52 / 41 | 90 / 56 | 107 / 74 |
| **L3** (3 синапса) | 0 / 3 | 0 / 6 | 0 / 6 |

Со второго этажа P9 управляется уверенно, с третьего сигнал умирает. **Практический предел глубины подключения — два синапса.** Реализовано как `--input-level {1,2}`.

## Асимметрия каналов — свойство коннектома, а не ошибка отбора

При переходе на L2 муха начала нарезать круги: путь 2.88 мм при +47.5° разворота даже без возмущения. Сначала грешил на своё правило отбора (правый набор исключал уже выбранный левый) — исправил на симметричное, веса выровнялись (278..133 против 288..132, пересечение 5 нейронов), но отклик остался неравным.

Автокалибровка (`--autocal N`: прогон на максимальном входе, потолок каждого канала измеряется отдельно) дала точную цифру: **левый канал 76.7 Гц, правый 120.0 Гц, асимметрия 1.57×**. Это реальное свойство подграфа коннектома. Общая константа нормировки для обоих каналов физически неверна — калибровать надо каждый канал по его собственному потолку.

Результат после калибровки (`L2_autocal`, одностороннее возмущение):

| Фаза | `cmd_L` | `cmd_R` | Скорость, мм/с | Поворот за 70 циклов |
|---|---|---|---|---|
| до | 0.730 | 0.837 | **14.06** | +18.2° |
| глушим левый | 0.055 | 0.971 | 9.20 | **+173.2°** |
| после | 0.593 | 0.822 | 12.25 | +47.8° |

Скорость сравнялась с конфигурацией L1 (14.06 против 13.48 мм/с), прямолинейность улучшилась вдвое (+18.2° против +47.5°). L2 стал рабочей конфигурацией.

Оговорка: покалиберная нормировка — это ещё одно рукотворное звено. Но оно хотя бы выведено из измерения, а не назначено, и его необходимость доказана числом 1.57.

---

# Аннотации типов клеток FlyWire (03.08.2026)

Скачаны `Supplemental_file1_neuron_annotations.tsv` (30.25 МБ) из публичного репозитория `flyconnectome/flywire_annotations` — аннотации из Schlegel et al., Nature 2024. Лежат в `~/fly-brain-data/annotations/`, в git не входят. Разбор: `tools/analyze_annotations.py`.

Валидация версии данных: совпало 139 248 из 138 639 наших нейронов; все 21 известных Sugar GRN опознаны как `sensory` / `gustatory` / `LB3`. Данные те же.

## Найдена ошибка: стороны P9 были перепутаны

| ID | Подпись в `fly-brain/code/benchmark.py` | Реальная сторона по FlyWire |
|---|---|---|
| `720575940627652358` | «P9 left» | **right** |
| `720575940635872101` | «P9 right» | **left** |

Контур унаследовал подписи из `benchmark.py` и гнал левые лапки от правого нисходящего нейрона. Это ровно то допущение, которое до сих пор честно помечалось как непроверяемое («сторонность проверить нечем без VNC»). Теперь оно проверено и оказалось неверным. Исправлено в `closed_loop_v2.py` и во всех измерительных скриптах.

**Что это меняет в уже полученных результатах:** величины, причинность и латеральная независимость остаются в силе — менять надо только подписи сторон. В прогонах `turn_left` и `L2_autocal` глушился на самом деле правый DNp09, поэтому направление поворота в тех записях следует читать зеркально.

## Что подтвердилось

Тип клетки у обоих нейронов — **DNp09**, канонический нисходящий нейрон ходьбы вперёд, медиатор ацетилхолин, выход через шейный коннектив. Выбор точки выхода был правильным.

## Что открылось

| Категория | Сколько в нашей модели |
|---|---|
| Нисходящие (выход из мозга) | **1299** (645 слева, 646 справа, 8 по центру) |
| **Восходящие** (вход из вентрального тяжа) | **1736** (838 слева, 898 справа), все через шейный коннектив |
| Сенсорные | 16 352 |
| — зрительные | 10 855 |
| — механосенсорные | 2 656 |
| — обонятельные | 2 279 |
| — вкусовые | 334 |

Нисходящих оказалось 1299, а не два — выходной интерфейс можно радикально расширить.

## Главная находка: DNp09 управляется ЗРЕНИЕМ, а не ногами

Гипотеза была такая: раз механосенсоры ног сидят в вентральном тяже, а в мозг их сигнал идёт через восходящие нейроны, то восходящие — правильная точка входа для обратной связи от лапок. Проверили измерением (`tools/ascending_to_dn.py`) — **не подтвердилась**:

| Популяция | Нейронов | Вход 50 Гц | 100 Гц | 200 Гц |
|---|---|---|---|---|
| Восходящие слева | 838 | 0.0 | 0.0 | 0.0 |
| Восходящие справа | 898 | 0.0 | 0.0 | 0.0 |
| Механосенсорные слева | 1363 | 0.0 | 0.0 | 0.0 |
| Механосенсорные справа | 1293 | 0.0 | 0.0 | 0.0 |

Шестнадцать условий, ни одного спайка DNp09 — при том что активируются тысячи нейронов (до 12 526 при 200 Гц). Сигнал по мозгу расходится, но до DNp09 не доходит.

Причину дал прямой разбор состава входов (`tools/dn_input_composition.py`):

| Класс входа | Нейронов | Доля \|веса\| | Алгебраическая сумма |
|---|---|---|---|
| `central` | 462 | 74.8% | **+12** |
| **`visual_projection`** | 111 | 22.8% | **+880** |
| `ascending` (от ног) | 16 | **1.8%** | −17 |
| `descending` | 15 | 0.5% | +17 |
| `motor` | 1 | 0.0% | +1 |

Числа для DNp09 left; у правого картина та же (visual_projection 26.0%, алгебраическая +1605, ascending 1.6% и −13).

Читается это так. Центральный вход составляет три четверти по модулю веса, но алгебраически почти обнуляется (+12 из 2932) — возбуждение и торможение уравновешены, это балансный вход, задающий состояние. **Единственный крупный источник чистого возбуждения — зрительные проекционные нейроны: +880 при 22.8% массы.** Вход от ног через восходящие — 1.8% и в сумме тормозный.

Самые сильные типы клеток на входе: **LC9** (50 нейронов, вес 385), **LC31a** (18, вес 338), LC31b, LCe04. Это лобулярные колончатые нейроны — зрительные детекторы признаков.

## Положительный контроль: зрение действительно зажигает DNp09

Отрицательные контроли сами по себе неполны — могло оказаться, что до DNp09 вообще ничем не достучаться, кроме его ближайших партнёров. Нужен положительный контроль, причём с популяциями, отобранными **по типу клетки из аннотаций, а не по силе связи с DNp09** (иначе вышло бы рассуждение по кругу). `tools/visual_to_dn.py`, в ячейках DNp09 left / right:

| Популяция | Нейронов | 50 Гц | 100 Гц | 200 Гц |
|---|---|---|---|---|
| **LC9 слева** | 87 | **32.0** / 0.0 | **61.0** / 0.0 | **109.0** / 0.0 |
| **LC9 справа** | 92 | 0.0 / **45.0** | 0.0 / **90.0** | 0.0 / **159.0** |
| LC31a обе стороны | 35 | 63.0 / 47.0 | 101.0 / 74.0 | 144.0 / 135.0 |
| Все зрит. проекц. слева | 4008 | **75.0** / 0.0 | **145.0** / 0.0 | **197.0** / 0.0 |
| Все зрит. проекц. справа | 4030 | 0.0 / **45.0** | 0.0 / **143.0** | 0.0 / **218.0** |

Отклик монотонный и сильный — до 218 Гц. Но важнее колонки нулей: **левое зрение управляет только левым DNp09, правое — только правым, перекрёстных наводок ровно ноль.**

Аргумент замкнут с обеих сторон: всё, что не зрение (восходящие, механосенсорные, вкусовые — 17 условий), даёт ноль; зрение даёт сильный ипсилатеральный отклик. **DNp09 — зрительный командный нейрон, и это установлено вмешательством, а не выведено из анатомии.**

Практическое следствие: латеральная развязка зрения идеальна, тогда как у подобранных вручную драйверов асимметрия усиления составила 1.57× и её пришлось вычищать автокалибровкой. Зрение даёт естественный сигнал поворота без всякой подгонки.

**Три следствия.**
1. Объясняется всё разом: почему Sugar GRNs не доходят до DNp09, почему стимуляция 1736 восходящих не даёт отклика, и почему подобранные вручную «прямые возбуждающие партнёры» всё-таки работали — они в основном и оказались зрительными LC9/LC31a. Мы вслепую нащупали правильный вход, не понимая, что это зрение.
2. Зрение (1442 омматидия) числилось в плане «отдельным будущим этапом». Данные говорят, что для этого нейрона это не опция, а основной канал.
3. Обратная связь от лапок в её нынешнем виде — вынужденная замена, а не приближение к биологии. Называть её «честной обратной связью» больше нельзя: правильнее сказать, что мы подаём сигнал о касании в нейроны, которые в живой мухе слушают глаза.

---

# План работ и критерии приёмки

| # | Этап | Критерий приёмки | Статус |
|---|---|---|---|
| 1 | Журнал соответствует фактам | — | сделано 02.08.2026 |
| 2 | Интерфейсы мозг↔тело выбраны по коннектому | точки подключения обоснованы данными | сделано 02.08.2026 |
| 3 | Модель мозга — валидированная `TorchModel` | воспроизводит сохранённый бенчмарк | сделано 02.08.2026 |
| 4 | Тюнинг-кривая P9 | монотонная зависимость, базовая частота измерена | сделано 02.08.2026 |
| 5 | Нормировка командного сигнала | разброс `cmd` больше нуля, без насыщения | сделано: 99 уровней из 100, насыщения нет |
| 6 | Мозг→тело через CPG | `pos_x_mm` не ноль и растёт | сделано: путь 17–33 мм |
| 7 | Тело→мозг: 6 флагов касания | флаги реальные, влияют на поведение | сделано: причинность доказана возмущением |
| 8 | Поворот под управлением пары P9 | разница cmd L/R даёт измеримый поворот курса | сделано: +138.6° против +14.1° |
| 9 | Более широкий репертуар входа | найти сенсорные популяции, реально достающие до DN | сделано: ответ — зрительные проекционные нейроны, а не восходящие |
| 10 | Скачать аннотации FlyWire | доступны super_class и cell_type | сделано 03.08.2026 |
| 11 | Исправить перепутанные стороны P9 | сторона выверена по аннотациям | сделано 03.08.2026 |
| 12 | **Подключить зрение мухи к LC-нейронам** | активность LC меняется при движении в поле зрения, DNp09 откликается | следующий, главный |
| 13 | Расширить выход за пределы пары DNp09 | использовать больше из 1299 нисходящих | ожидает |
| 14 | Мост в Minecraft | — | фундамент готов |

## Что осталось честно оговорить
- Связка «левый P9 → левые лапки» выбрана нами, а не выведена из коннектома: VNC в датасете нет, проверить сторонность по данным нечем. Это ровно тот класс ручных допущений, который зафиксирован в разделе про прецедент Eon Systems.
- Основная «разумность» походки по-прежнему в CPG-контроллере тела, а не в мозге. Мозг задаёт режим (идти / не идти / насколько быстро), и это подтверждено измерением. Называть это «мухой, которая думает» нельзя.
- Обратная связь идёт в прямых партнёров P9 — это реальная связь из коннектома, но выбор именно топ-20 по весу сделан нами.

## Важные технические заметки
- Смонтированные диски под WSL (`/mnt/...`) — это 9p, медленнее нативного ext4 примерно втрое для файлового I/O. Тяжёлые данные (parquet, pkl, веса) держать в домашнем каталоге. Код, логи и результаты можно на смонтированном диске.
- WSL2 по умолчанию берёт до 50% RAM хоста — при 16 ГБ это ~8 ГБ, чего впритык не хватает для GeNN на полном графе.
- Копия этого журнала обнаружилась на рабочем столе Windows под именем вида `D<путь>PROJECT_LOG.md` — файл был сохранён так, что полный путь попал в имя, а символы `:` и `\` оказались заменены на U+F03A/U+F05C (так WSL кодирует запрещённые в Windows символы). Содержимое разобрано выше и учтено; сам файл можно удалить.
